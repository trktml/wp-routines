{
  "name": "wp-routines",
  "version": "1.0.0",
  "description": "Whatsapp cron reminder",
  "main": "app.ts",
  "scripts": {
    "start": "swc ./src -d dist && cls && node dist/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "whatsapp",
    "whatsapp-web.js",
    "whatsapp",
    "routines",
    "bot"
  ],
  "author": "Tarık Temel",
  "license": "MIT",
  "dependencies": {
    "whatsapp-web.js": "^1.22.1"
  },
  "devDependencies": {
    "@swc/cli": "^0.1.62"
  }
}